# Vacway Frontend

Prerequisites:

- Node (>= 6.x)

## Development guide

First of all install the required dependencies:

```sh
npm install
```

Then, you can start the frontend like this:

```sh
npm start
```
